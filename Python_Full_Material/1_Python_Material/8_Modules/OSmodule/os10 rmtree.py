
#removing a directory with content : for this we need to import shutil module ,
#    which consists of rmtree() function , using which we can remove a directory

import shutil
import os

shutil.rmtree("sampledir")
