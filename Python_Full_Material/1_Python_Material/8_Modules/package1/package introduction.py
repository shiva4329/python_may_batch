#Packages :

A Package is a folder or directory which is a collection of python modules.

-To treat a folder or a directory as a package, it should have a special file called__init__.py file

-A package can contain modules and sub-packages and each sub-package again should contain __init__.py
 file.

 Accessing a property from  sub-package:

     packagename.subpackage.modulename.propertyname
